# Backend origin (the app talks to it via the same-origin /api/v1/ proxy).
VITE_SERVER_URL=https://isourceplus.net/

# Pre-launch waitlist toggle.
#   true  -> every route serves the waitlist landing page
#   false -> the real application
# Must also be set in the Render environment for production builds.
VITE_WAITLIST_MODE=false
